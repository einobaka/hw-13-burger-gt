<h1>Burgers!</h1>

<h2>Burgers that need to be devoured!</h2>

<ul>
    {{#each burger}}
    {{#unless devoured}}
    {{> burgerblock devoured=1 }}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that were devoured...</h2>

<ul>
    {{#each burger}}
    {{#if devoured}}
    {{> burgerblock devoured=0 }}
    {{/if}}
    {{/each}}
</ul>

<h2>Buy and add a burger on the yummy list</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="burg" name="name">
    </div>

    <div class="form-group">
        <label for="burgerlist">Is it devoured?</label><br>
        <input type="radio" name="devoured" value="1" checked> Heck yes!<br>
        <input type="radio" name="devoured" value="0"> Not yet...
    </div>

    <button type="submit">Add Burger!</button>
</form>